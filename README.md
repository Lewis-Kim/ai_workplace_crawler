# ai_workplace_crawler

``` 사용법
python -m venv venv         -> 가상화
venv\Scripts\activate       -> 활성화

pip install -r .\requirements.txt   -> 라이브리 인스톨 작업

프로젝트 폴더에 .env파일 생성
DB_HOST=
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_CHARSET=utf8mb4

OPENAI_API_KEY=

를 복사해서 디비 정보를 넣고 저장